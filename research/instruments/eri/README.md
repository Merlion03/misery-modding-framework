# ERI — External Read-Only Inspector — заглушка (кода нет)

> **СТАТУС: КОДА В ЭТОМ КАТАЛОГЕ НЕТ.**
> Инструмент не написан. Ни один его запуск не производился, ни один дамп им не получен.
> Отсутствие кода — текущая правда: язык и технология инструмента ещё не выбраны (открытый
> вопрос Q-8.1), а сам выбор делается в M3. Каталог создан задачей R-02 (plan.md §1.4) ради exit
> criterion «все файлы из §9.2 существуют, пусть и с заголовками-заглушками».

## RESEARCH ONLY — NOT PRODUCTION

Это пометка, обязательная по plan.md §8.1, и она относится ко всему, что здесь когда-либо
появится. Правило секции: **ни ERI, ни IPP не наследуются продуктом**. Код обоих живёт в
`research/instruments/`, а не в `src/`, и в Phase 2 создаётся **новый** проект, а не рефакторинг
любого из них. В `docs/phase1-report.md` обязателен раздел «чему из ERI/IPP нельзя подражать».

Свойства, которые здесь считаются нормой, а в продукте были бы дефектом (plan.md §8.1):
одноразовость и выбрасываемость, отсутствие какой-либо стабильности API, и обработка ошибок в
режиме «падать громко и сразу», а не «деградировать безопасно».

## Что здесь будет

Отдельный процесс, который открывает процесс игры **с правами только на чтение**
(`PROCESS_QUERY_INFORMATION | PROCESS_VM_READ`), перечисляет модули и читает память. Он
**ничего не пишет, ничего не инжектит, не ставит хуков и не вызывает функций игры** — это
архитектурное свойство, а не настройка (plan.md §8.2).

Это уровень 1 инструментария и точка, с которой начинается любое наблюдение за живой игрой.
Правило по умолчанию plan.md §8.4: **если задачу можно решить уровнем 1 хотя бы дороже — она
решается уровнем 1.** Экономия времени исследователя основанием для перехода на уровень 2 не
является.

## Возможности, которые предстоит реализовать (plan.md §8.2)

| ID | Возможность | Кого обслуживает |
|---|---|---|
| I-01 | Найти процесс, получить базовый адрес и размер образа Shipping-модуля | всё |
| I-02 | Перечислить объекты через кандидатный `GUObjectArray` | RF-10 |
| I-03 | Разрешить `FName` в строку (обход `FNamePool`) | RF-11 |
| I-04 | Дамп `UClass` с иерархией наследования | RF-12 |
| I-05 | Дамп `UFunction` (имя, флаги, параметры) | RF-12 |
| I-06 | Дамп `FProperty` (имя, тип, offset, размер, флаги, порядок в классе) | RF-12, §14A |
| I-07 | Найти `UWorld` | M4 |
| I-08 | Найти `UGameInstance` и его subsystems | M4 |
| I-09 | Найти `ULocalPlayer` и локальный `APlayerController` | M4 |
| I-10 | Найти локального player character (Pawn) | M4 |
| I-11 | Читать значения свойств конкретного объекта | M6 |
| I-12 | Читать `Role` / `RemoteRole` / `NetMode` / ownership | M8 |
| I-13 | Периодические снимки и diff между ними | M6, M8 |
| I-14 | Прочитать список смонтированных контейнеров и состояние pak-платформы | M5a |
| I-15 | Прочитать содержимое AssetRegistry в памяти | M2, M5b, CR-01 |
| I-16 | Экспорт собранного в `research/` как JSONL | всё |

Ожидаемое покрытие уровня 1 (plan.md §8.2): M3, M4 и M8 полностью; M5a — при условии, что
сборка обнаруживает контейнеры автоматически; M6 — в части чтения состояния.

Отдельно стоит запомнить следствие I-14: факт монтирования внешнего контейнера проверяется
**не запуская код внутри игры** — положить тестовый контейнер туда, где игра сама его подхватит,
запустить игру обычным способом и прочитать список смонтированных контейнеров извне. Если
автоматическое обнаружение работает, весь M5a закрывается уровнем 1.

## Кто и когда заполняет

- Milestone: **M3** — там же выбирается технология (plan.md §8.6, Q-8.1: кандидаты — Python с
  `ReadProcessMemory` через `ctypes`, Rust, C++; критерий выбора — минимальная стоимость до
  первого дампа)
- Предпосылки: RF-04..RF-08 дают гипотезы, которые есть что проверять, и закрыты гейты Q-8.2
  (анти-отладка) и Q-8.3 (анти-чит) — plan.md §8.6 и §18.2. Ни одно из условий не выполнено.
- Q-8.4 больше не открытый вопрос, а проектное решение: начинаем с уровня 1 и считаем его
  достаточным для M3 и M4, пока конкретная задача не докажет обратное.

## Правила, действующие для запуска (plan.md §8.5)

* Инструмент запускается **только** против полностью контролируемых сессий (решение D-05).
  Никаких публичных лобби.
* Все дампы пишутся в `research/` этого репозитория и **никогда** в папку игры (решение D-01).
* Ни один инструмент не устанавливается «навсегда»: постоянное присутствие наших файлов в папке
  игры запрещено.
* Каждый запуск пишет манифест в `research/instrument-runs/<timestamp>/manifest.json` — см.
  `research/instrument-runs/README.md`.

## Что означает «достаточно»

plan.md §8.6, exit criteria M3: ERI выдаёт `classes.jsonl` с непустым `/Script/MISERY`,
воспроизводимо, **без единой записи в память процесса** и без записи чего-либо в папку игры.

Обратить внимание, что критерий здесь предъявляется к способу получения результата не меньше, чем
к результату. Дамп, полученный записью в процесс, критерий не выполняет — он его нарушает.

## Каркас будущего документа этого каталога

Когда появится код, README дополняется разделами:

```markdown
## Технология и почему выбрана (ответ на Q-8.1)
## Как запускать
## Какие I-* реализованы, какие нет
## Что инструмент принципиально не делает (и почему это архитектурно)
## Известные ограничения
## Чему из этого кода нельзя подражать в Phase 2
```
