# Components — заглушка (файл пуст)

> **СТАТУС: ПУСТО. НИ ОДНОГО ФАКТА ОБ ИГРЕ ЗДЕСЬ НЕТ.**
> Отсутствие содержания — текущая правда: подсистема не исследовалась. Файл создан задачей
> R-02 (plan.md §1.4) ради exit criterion «все файлы из §9.2 существуют, пусть и с
> заголовками-заглушками». Он не является ни выводом, ни гипотезой, и на него нельзя
> ссылаться как на источник. Пока стоит эта пометка, единственный корректный ответ на любой
> вопрос ниже — «не исследовано».

## Что здесь будет

Композиционная модель сборки: какие собственные `UActorComponent` существуют в модуле
`/Script/MISERY` и в Blueprint-классах, и как они связаны с персонажем игрока. Этот файл задаёт,
на что вообще похож будущий API — на набор компонентов или на что-то иное.

## Кто и когда заполняет

- Подсистема: **6** (plan.md §11.1)
- Milestone: **M6**, обязательный минимум (plan.md §18.2, критерий (1) выхода из M6)
- Инструмент: карта reflection из M3 плюс чтение состояния через ERI (plan.md §8.2)
- Предпосылки: заполненные подсистемы 1..5 (M4) и вердикт M5c, задающий глубину M6
- Порядок: M6 идёт после M5a/M5b/M5c (plan.md §11, приоритетное правило v2)

## Вопросы, на которые обязан ответить этот файл

Из plan.md §11.1, строка 6:

1. Какие кастомные `UActorComponent` есть в `/Script/MISERY` и в Blueprint?
2. Как они связаны с персонажем?

## Что нужно собрать

Из plan.md §11.1, колонка «Данные»: список классов компонентов и их владельцев.

## Что означает «достаточно»

Из plan.md §11.1, колонка «Достаточно =»: составлена карта «actor → components» для игрока.

Формальные пороги заполненности заданы в plan.md §11.1 «Exit criteria» и §18.2, критерий (1)
выхода из M6, и здесь намеренно не воспроизводятся ни словом-уровнем, ни числом — см.
`research/systems/README.md`.

## Что это даст будущему SDK

Из plan.md §11.1: композиционная модель API.

## Каркас будущего документа (обязательный шаблон plan.md §11)

```markdown
# Components
## Questions to answer
## Data to collect
## Method
## Findings (evidence level, confidence, build_key)
## What is "enough" (definition of done)
## Implications for future SDK
## Open unknowns
```

## Правила заполнения

Общие для всего каталога — см. `research/systems/README.md`, раздел «Правила, действующие при
заполнении». Раздел «Implications for future SDK» обязателен у каждого заполненного файла M6
(plan.md §18.2, критерий (3)).
