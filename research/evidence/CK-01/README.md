# CK-01 — читаемость контейнеров и границы того, что об этом гейте можно измерить

Каталог держит две волны работы. **Волна 1** отвечает на вопрос, который стоит **перед**
CK-01: можно ли вообще прочитать содержимое `MISERY-Windows.pak` в границах решения D-02.
**Волна 2** закрывает сам гейт настолько, насколько он закрываем: разбирает механизм по
первоисточнику UE 5.4.4, ограничивает возможное со стороны образа и записывает вердикт.
Сводный документ волны 2 — `research/modkit/ck-01.md`.

## Что здесь лежит

| Файл | Что это |
|---|---|
| `pak-index.json` | Полный документ инструмента `tools/content/pak_index.py`: футер, доказательства plaintext для трёх блоков индекса, разбор 4424 записей, четыре опровергающих пробы, слой class P (literal_reads) и слой class I |
| `pak-paths.txt` | Список всех 4424 путей: размер, размер без сжатия, флаг шифрования, индекс метода сжатия, путь. Только метаданные — ни один байт payload не читался |
| `pak-index-run1.log` | Прогон 1, с записью артефактов |
| `pak-index-run2.log` | Прогон 2, без записи. Два прогона дают идентичный JSON, кроме `generated_at` |
| `tu-literals-shipping.json` | Волна 2. Документ `tools/static/tu_literals.py` по Shipping-образу: перечёт литералов путей исходников по всему файлу, запросы, **измеренная сила пробы** и четыре опровергающих пробы |
| `tu-literals-shipping-paths.txt` | 549 относительных путей исходников, найденных в Shipping-образе: число вхождений, первое смещение, кодировки, путь. Префикс build root вынесен в JSON и в прозе не повторяется (C-13) |
| `tu-literals-shipping-run1.log`, `-run2.log` | Два прогона по Shipping-образу; документы совпадают побайтово, кроме `generated_at` и `timings_seconds` |
| `tu-literals-d04-oracle.json` | То же по `MISERY.exe` — оракулу D-04. **Другой бинарник**; служит контрастом, который измеряет механизм источника литералов, а не назначает его |
| `tu-literals-d04-oracle-run1.log`, `-run2.log` | Два прогона по оракулу D-04 |
| `zen-plaintext-probe.py` | Волна 2. Проба, атакующая вердикт BLOCKED собственного документа: находит настоящие chunk-и `ExportBundleData`, читает их первые байты там, где они физически лежат, и проверяет на `FZenPackageSummary`. Три перекрёстные проверки разбора и положительный контроль — `global.ucas`. Скрипт, а не модуль в `tools/`, потому что этой поверхностью уже владеют `tools/content/iostore_chunks.py` и `tools/content/package_summary.py`; перенос туда назван как follow-up |
| `zen-plaintext-probe-run1.log`, `-run2.log` | Два прогона пробы. Вывод намеренно без времени, поэтому протоколы **побайтово идентичны** |

Полный перечень путей для оракула D-04 не выписан намеренно: их 6093, а довод, ради
которого он понадобился бы, уже сделан двумя литералами, названными в
`research/modkit/ck-01.md` §4.2.

## Главный результат

**Все 4424 записи зашифрованы.** Флаг читается из бита 22 закодированного слова
(`IPlatformFilePak.cpp:7174`), который выставляет `FPakEntry::Flag_Encrypted == 0x01`
(`IPlatformFilePak.h:382`). Различных слов флагов в контейнере ровно **одно** —
`0xE0400000`, 4424 раза.

Незашифрованных записей — **0**, поэтому ни один payload этим прогоном не читался:
D-02 разрешает читать только то, что доказано незашифровано.

**Cooked-пакетов в контейнере нет ни одного:** `.uasset`, `.uexp`, `.umap`, `.ubulk`,
`.uptnl`, `.usmap`, `.ushaderbytecode` — по нулю. CK-01 из этого контейнера не
отвечается ни в какую сторону, ни при каком ключе.

## Почему чтение индекса не является расшифровкой (D-02)

Оракулом здесь выступает сам движок. `FPakFile::DecryptAndValidateIndex`
(`IPlatformFilePak.cpp:6489-6500`) расшифровывает буфер **тогда и только тогда**, когда
`Info.bEncryptedIndex`, и затем требует `SHA1(buffer) == StoredHash`. В этом файле
`bEncryptedIndex == 0`, и SHA1 байтов **как они лежат на диске** совпадает с
записанным хэшем для всех трёх блоков (primary 53 202, path-hash 133 821, full
directory 103 835 байт). Значит байты, которые разбирает движок, — это байты, которые
прочитали мы, без шага расшифровки между ними: иначе загрузка файла самим движком
провалилась бы. Запас проверки — 160 бит.

На payload этот аргумент не переносится, и инструмент этого не делает.

## Четыре пробы, каждая способна опровергнуть вердикт

1. `entry_flag_word_census` — перечень всех различных слов флагов с полным разбором.
   Ошибка маски в сводке невидима, в перечне — видна. Индекс метода сжатия
   перепроверяется против таблицы `CompressionMethods` футера.
2. `encoded_blob_tiling` — 4424 записи покрывают 53 088 байт блока **ровно**: разрывов 0,
   перекрытий 0. Неверная ширина полей не замостит блок точно случайно.
3. `layout_probe` — не читает ни одного флага. Зашифрованная несжатая запись занимает на
   диске `Align(Size, 16)` байт при том, что поле `Size` хранит длину открытого текста
   (`PakFileUtilities.cpp:1028, 1044-1060`); незашифрованная — ровно `Size`. Модель с
   выравниванием объясняет 2405 промежутков точно, модель без выравнивания — **0**,
   перекрытий 0, а конец последнего payload с выравниванием попадает в начало индекса
   **точно** (117 365 605), тогда как без выравнивания недостаёт 3 байт.
4. `local_header_probe` — локальный заголовок `FPakEntry`, который упаковщик пишет
   открытым текстом (`PakFileUtilities.cpp:2736`, шифруется только буфер данных, :1057 и
   :1109). Это независимая копия того же байта флагов, сделанная другим сериализатором в
   другом месте файла. Прочитано 4424 заголовка, расхождений с индексом **0**, байт
   флагов `0x01` у всех.

## Волна 2 — что она добавила и чего не смогла

**Добавила четыре вещи.** Первая: сам гейт CK-01 отвечается **не образом** — флаг
`PKG_UnversionedProperties` есть свойство контента, он пишется в summary каждого пакета
(`LinkerSave.cpp:528-550`) и читается оттуда на каждый пакет отдельно
(`LinkerLoad.cpp:1421-1423`, `AsyncLoading2.cpp:6205`). Вторая: единственный
незашифрованный контейнер установки — `global.utoc`/`global.ucas` — содержит одну запись,
и её тип прочитан как `0x05` = `EIoChunkType::ScriptObjects` (`IoChunkId.h:33`), то есть
пакетов там нет и маршрут закрыт по всей поверхности, а не «пока не проверен». Третья:
дешёвая резкая проба «есть ли литерал пути `UnversionedPropertySerialization.cpp` в
образе» **измерена на силу** и оказалась бессильной в отрицательную сторону — из 595
наверняка слинкованных translation unit без фатальной конструкции литерал в
Shipping-образе оставил **один**, а 594 не оставили ничего. И четвёртая, добавленная
после первой редакции: слово «зашифрован» само проверено, а не взято с флага —
8 из 8 прочитанных chunk-ов `ExportBundleData` не разбираются как заголовок пакета,
энтропия 1 МиБ контейнера 7,9699 и 7,9954 бит на байт по двум смещениям, тогда как
положительный контроль `global.ucas` в том же формате даёт 6,3649 и структурные
первые байты.

**Не смогла** повысить оценку самого вердикта выше 0.75: второго независимого метода для
утверждения о настройке cook-а не существует в границах D-02, потому что измерять нечего —
настройка живёт в зашифрованном контенте. Гейт остаётся не снят, закрывающие методы
названы и оценены по стоимости в `research/modkit/ck-01.md` §7.

## Что здесь НЕ доказано

Совпадает ли набор путей pak с набором чанков IoStore: directory index
`MISERY-Windows.utoc` зашифрован и не читается, поэтому сравнение множеств путей
невозможно. Утверждение о непересечении делается только по классу содержимого.

Что translation unit `UnversionedPropertySerialization.cpp` слинкован именно в
Shipping-образ: он подтверждён в `MISERY.exe`, а это другой бинарник (D-04). Перенос
делается посылкой «тот же исходник, тот же changelist, объявление без `#if`», и посылка
названа, а не спрятана.
