# research/unknowns.md — реестр открытых вопросов

Создан: 2026-08-22 (M0, задача R-02..R-05).
Источник состава: `plan.md` Приложение B, а также таблицы вопросов §2.3, §3.3, §5.3, §7.3, §8.6, §12.1, §13.1/§13.2, §14.3, §14.4, §14.7, §14A.4, §16.3, §17.4.

---

## Контракт файла

Это **не копия Приложения B**, а рабочий реестр. Правила его ведения обязательны.

1. **Append and update, never silently delete.** Запись не удаляется никогда. Если вопрос оказался
   некорректно поставленным — он **переформулируется в той же записи**, с сохранением старой
   формулировки в поле заметок. Удаление записи без явного решения в `research/decisions.md` запрещено.
2. **ANSWERED остаётся в файле как история.** Отвеченный вопрос переводится в статус `ANSWERED`,
   в заметки добавляется: дата, **ссылка на артефакт**, который дал ответ (путь в `research/` или `docs/`),
   достигнутый `evidence_level`, `confidence` и фактический `oracle`. Ответ без ссылки на артефакт
   ответом не считается.
3. **Слабый oracle не закрывает вопрос.** Если полученный ответ подтверждён oracle, которого
   недостаточно по матрице «тип утверждения → необходимые oracle» (`plan.md` §10.5), запись
   **остаётся открытой** (`IN PROGRESS`) с пометкой, какой именно oracle ещё нужен. Типовые случаи:
   утверждение «функция X делает Z» требует `binary-analysis` **и** подтверждения наблюдением;
   CR-01 требует минимум двух oracle; E-3b требует фактического запуска — никакой статический
   анализ его не закрывает.
4. **Никакая догадка не записывается как факт.** В колонке заметок догадка либо помечена словом
   «догадка» с указанием основания, либо стоит «оснований для догадки нет».
5. **Новые UNKNOWN, найденные по ходу работы, вносятся сюда, а не забываются** (`plan.md` §18.3 п.3:
   milestone не закрывается, пока это не сделано). Вопросы, которых нет в плане, вносятся **только**
   в раздел «Вновь выявленное» в конце файла и до утверждения владельцем не смешиваются с
   утверждённым набором.
6. **Статусы:** `UNKNOWN` (работа не начата), `IN PROGRESS` (работа идёт, либо ответ получен частично /
   слабым oracle), `ANSWERED` (ответ есть, артефакт есть, oracle достаточен).
7. **ID не переиспользуются.** ID взяты из плана и остаются неизменными; переименования (как
   `E-3a` → `SP-1`, старые `E-5`/`E-6` → `E-6`/`E-7`) фиксируются в заметках, а не создают вторых записей.
8. Пять гейтовых вопросов присутствуют в файле **дважды**: в верхней таблице гейтов (с колонкой
   последствий) и в своей тематической таблице (с полным набором полей). Это сделано намеренно.

**Словарь `oracle` (обновлено 2026-08-22).** `plan.md` §10.5 (правка v2.1) расширил перечисление с
шести значений до девяти, добавив `filesystem`, `steam-metadata` и `vcs-history`. Полный список:
`filesystem`, `steam-metadata`, `vcs-history`, `global-ucas`, `asset-registry`, `runtime-reflection`,
`binary-analysis`, `container-metadata`, `external-doc`. Соответственно в этом файле нормализованы
черновые пометки: «инвентарь файлов установки (вне матрицы)» → `filesystem`; «метаданные Steam
(вне матрицы)», `appmanifest` → `steam-metadata`. Значение `n/a` в поле `oracle` **запрещено**:
вместо него указывается фактически использованный oracle.

Обозначение `oracle: вне матрицы §10.5` сохраняется только там, где вопрос вообще не является
утверждением о данных: инженерный выбор, решение владельца, измерение характеристик нашего
собственного инструмента. Такие записи не проходят через линтер EV-04 сознательно — это не пробел.
Отдельно остаётся **не** закрытый правкой v2.1 класс: наблюдения уровня ОС (дерево процессов,
launch options) и **содержимое** crash-отчётов и логов движка — см. NEW-01.

---

## 1. ГЕЙТЫ — пять вопросов, от которых зависит форма проекта

Порядок следования = порядок влияния. Колонка «если ответ плохой» — главное содержание этой таблицы.

| ID | Вопрос | Статус | Milestone | Oracle (§10.5) | Если ответ плохой |
|---|---|---|---|---|---|
| **Q-8.3** | Есть ли у сборки анти-чит (EAC/BattlEye) или иная защита исполнения? В recon их файлов в папке игры не найдено, но это не доказательство отсутствия | UNKNOWN | **M1** | `binary-analysis` (импорты, TLS-callbacks, строки) + `filesystem` (инвентарь файлов установки) | Если анти-чит есть: уровень 2 (§8.3, IPP) пересматривается целиком, уровень 1 (ERI) переоценивается отдельно, под вопросом оказываются M3/M4/M6/M8 и сама идея in-process Runtime в Phase 2; приоритет уходит в офлайн-методы (RF-01, §7), а целевой UX «просто запусти через Steam» может стать недостижимым |
| **CK-01** | Использует ли сборка unversioned property serialization в cooked-пакетах? | UNKNOWN | **M2s** | `binary-analysis` + `container-metadata` | Если используется, а схему свойств мы воспроизвести точно не можем: пакет читается по позиции, а не по имени, и любое расхождение в наборе/порядке свойств stub-а даёт **тихую порчу данных вместо честной ошибки** (RISK-15, худший исход). Mod kit §14A в текущем виде становится нереализуемым |
| **CK-04** | Офсеты свойств дочернего BP-класса **запекаются** при cook или пересчитываются при загрузке (`Link()`)? | UNKNOWN | **M2s** (через SP-1 / CT-04) | `binary-analysis` | Если запекаются: stub обязан совпадать с реальным layout-ом native-класса байт-в-байт, а reflection даёт его лишь приближённо → E-3b почти наверняка `fail` или, хуже, «принят и прочитан неверно». Трек §14A либо закрывается, либо сводится к задаче точного воспроизведения заголовков. Ответ приходит **до** траты 40 ГБ на UE (D-06) — в этом его ценность |
| **CR-01** | Как MISERY получает список предметов и прочих content definitions: DataTable? DataAsset? `UAssetManager` PrimaryAssetType? собственный registry? жёстко закодированный список? | UNKNOWN | **M3** (1-й проход, целевая confidence 0.5–0.7) → **M5b** (подтверждение ≥0.8) | `runtime-reflection` + `asset-registry` (минимум два oracle по §10.5), желательно + `binary-analysis` | Если список жёстко закодирован и точки расширения нет: mounting может работать, а моды всё равно невозможны — реализуется RISK-05. Ветка content-modding меняет форму целиком, проект сводится к behaviour-моддингу через reflection |
| **E-3b** | Загружается ли в **оригинальной** игре cooked BP, унаследованный от настоящего native-класса из `/Script/MISERY`, с корректным наследованием и осмысленным доступом к унаследованным native-свойствам и функциям? | UNKNOWN | **M5c** (стадия MK-3) | **эксперимент (фактический запуск)** + `runtime-reflection`; по §10.5 одного oracle недостаточно, и никакой статический анализ этого не заменяет | `fail` = meaningful custom content, наследующийся от игровых классов, невозможен. При `E-3a pass` проект переориентируется на behaviour-моддинг — это **другой, гораздо более скромный продукт**, и §14A.7 требует назвать это вслух. `E-3a fail` тоже = внешний контент не грузится вовсе, §14 закрывается отрицательно. Смягчающие формулировки («частично работает», «требует доработки») прямо запрещены §18.3 |

**Порядок снятия гейтов** (Приложение C): Q-8.3 первым (инструмент готов, ответ достижим сразу и
дешёво) → CK-01 и CK-04 статическим анализом в M2s → первый проход CR-01 сразу по получении
reflection map в M3 → E-3b последним, в M5c, потому что он требует всего остального.

---

## 2. Инструментальные вопросы

| ID | Вопрос | Статус | Milestone | Что блокирует | Oracle (§10.5) | Заметки / догадка |
|---|---|---|---|---|---|---|
| Q-8.1 | Язык и технология External Read-Only Inspector (ERI) | UNKNOWN | M3 | Старт M3: без ERI нет reflection-дампа, а значит нет M4, M8 и второго прохода CR-01 | вне матрицы §10.5 — инженерный выбор, а не утверждение об игре | Кандидаты: Python + `ReadProcessMemory` через `ctypes`, Rust, C++. Критерий выбора зафиксирован планом: минимальная стоимость до первого дампа. **Догадка** (основание — вся остальная оснастка на Python): Python + ctypes. Решение принимается в M3 и записывается в `decisions.md` |
| Q-8.1b | Язык и технология In-Process Research Probe (IPP) | UNKNOWN | только при эскалации по §8.4 | Ничего. Сознательно **не** решается заранее (§8.6) | вне матрицы §10.5 — инженерный выбор | Выбирать заранее запрещено планом. Любой преждевременный выбор здесь — нарушение §8.4 |
| Q-8.2 | Есть ли у игры анти-отладочная / анти-инъекционная защита | UNKNOWN | M1 | Пункт 3 критериев эскалации §8.4: без ответа переход на уровень 2 запрещён в принципе. Влияет на стоимость §7 | `binary-analysis` (импорты, TLS-callbacks, характерные строки) | Оснований для догадки нет: импорты/экспорты/TLS/debug directory ни у одного exe не разбирались (Приложение A, «что НЕ сделано») |
| **Q-8.3** | Есть ли анти-чит (EAC/BattlEye) | UNKNOWN | M1 | **ГЕЙТ для §8 целиком** — см. таблицу гейтов | `binary-analysis` + `filesystem` (инвентарь файлов установки) | Отсутствие файлов анти-чита в папке игры (recon) — слабый негативный признак, не доказательство. Догадка «анти-чита нет» имеет низкую уверенность и не может использоваться как основание для эскалации |
| Q-8.4 | Достаточно ли уровня 1 (ERI) для M3/M4 | **ANSWERED** (проектное решение) | — | Ничего | не применим — проектное решение, не факт об игре | План §8.6: «больше не открытый вопрос, а проектное решение»: начинаем с уровня 1 и считаем его достаточным для M3/M4, **пока не доказано обратное конкретной задачей**. Артефакт: `plan.md` §8.4/§8.6. Запись **переоткрывается** при первом конкретном вопросе, недостижимом уровнем 1, с обязательной записью в `research/decisions.md` по процедуре §8.4 |
| Q-8.5 | Достаточно ли уровня 1 для M5a — то есть хватает ли возможности I-14 (прочитать список смонтированных контейнеров извне) | UNKNOWN | M5a | Решение об эскалации на IPP (возможность P-03 «инициировать mount») и тем самым объём §8 | `runtime-reflection` (I-14) | Зависит от CL-03 / Q-2.3: если сборка обнаруживает контейнеры автоматически, весь M5a закрывается уровнем 1. Оснований для догадки нет |
| S-10 | Присутствует ли в exe игры MSVC RTTI (`.?AV...@@`) | UNKNOWN | M1 (выполнить **рано**) | Стоимость §7 меняется на порядок; скорость M2s и всей идентификации классов | `binary-analysis` (`tools/static/rtti_scan.py`; Ghidra `Windows x86 PE RTTI Analyzer` присутствует и запускается автоматически — A-15) | **Догадка низкой уверенности**: UE по умолчанию собирается без RTTI, поэтому ожидается отрицательный ответ. Основание — общая практика UE, а не наблюдение этой сборки. На бинарниках игры Ghidra ещё не запускалась |
| T-05 | Сколько занимает Ghidra-проект для 134-МБ `MISERY-Win64-Shipping.exe` и сколько идёт полный анализ | UNKNOWN | M2s | Практическую выполнимость §7 и M2s на этой машине (RISK-13: на `C:` ~3.2 ГБ, кэши Ghidra живут в `%APPDATA%\ghidra` и `%LOCALAPPDATA%\ghidra`) | вне матрицы §10.5 — измерение нашего инструмента | Оснований для количественной догадки нет. Стратегия задана планом заранее: первый импорт с `-noanalysis`, затем выборочные анализаторы; проект хранить на `D:`; при переполнении — анализ по секциям/диапазонам адресов |

---

## 3. Сборка и бинарники

| ID | Вопрос | Статус | Milestone | Что блокирует | Oracle (§10.5) | Заметки / догадка |
|---|---|---|---|---|---|---|
| A-03 | Почему ни у одного из трёх exe не читаются поля `VS_VERSIONINFO`, при том что секция `.rsrc` присутствует | UNKNOWN (причина) | M1 | Полноту поля `pe.version_info` в `fingerprint.json` (§3.1). Без ответа поле остаётся `null` со ссылкой на эту запись | `binary-analysis` — прямой разбор `.rsrc` нашим PE-парсером (F-01) | Факт пустоты — OBSERVED, confidence 0.90. **Сверка с источником 2026-08-22 (LOG-0012):** здесь стояло 0.95; факт градуирован в `plan.md` A-03, и там он стоит на 0.90, потому что для 0.95+ обязательны все шесть критериев класса I, а сырого артефакта, второго прогона и попытки опровержения у него нет. Значение приведено к источнику, а не назначено заново (§10.1: пересказ не повышает и не понижает). **Догадка**: ресурс `VERSIONINFO` просто не заполнен (UE не заполняет его без настроек проекта), а не повреждён. Проверяется разбором `.rsrc`, а не `Get-Item().VersionInfo` |
| A-05 | Что такое `MISERY\Binaries\Win64\MISERY.exe` — 282 826 240 B, 10 секций включая `.uedbg`, PE link timestamp 1784645631, **отсутствует** в `Manifest_NonUFSFiles_Win64.txt` | UNKNOWN (интерпретация; сами факты OBSERVED, confidence 0.85 — сверено с `plan.md` A-05 2026-08-22, LOG-0012; здесь стояло 0.99) | M1 | Ничего напрямую — роль файла уже ограничена решением D-04. Блокирует только попытку опереться на него шире, чем D-04 разрешает | `binary-analysis` + сравнение с non-UFS манифестом (`container-metadata` здесь не применим) | Почему 0.85, а не 0.99: строка градуируется по своей слабейшей части, а слабейшая здесь — число секций и значение timestamp, полученные одиночным ручным чтением recon; `notes.md` сборки прямо помечает счётчики секций как требующие перепроверки F-01. Правка v2.4 §10.3 этого понижения не снимает: клетка называет секцию `.uedbg` и поле `TimeDateStamp`, то есть говорит, **чем** прочитанное является, и опирается на внешнюю раскладку PE, а смещений у этого чтения recon не записал (`plan.md` A.6, случай (c)). **HYPOTHESIS «в депот попала Development-сборка игры», confidence 0.65.** По D-04 называть это фактом запрещено; файл разрешён как read-only oracle, **не** является target для bindings, и каждый полученный на нём вывод обязан быть перепроверен на Shipping-бинарнике (RISK-07) |
| A-12 | Почему в `global.ucas` присутствуют имена **редакторных** модулей (`/Script/TranslationEditor`, `/Script/ChaosEditor`, `/Script/AssetManagerEditor`, `/Script/InterchangeEditor` и др.) | UNKNOWN (интерпретация; факт OBSERVED, confidence 0.75) | M2 | Интерпретацию всего инвентаря RF-01: какие из 394 найденных `/Script/<Module>` реально входят в сборку. Связано с A-09 | `global-ucas` (доказывает только наличие имени) + `binary-analysis` (есть ли в exe соответствующий код) | **Сверка с источником 2026-08-22 (LOG-0012):** здесь стояло 0.9; факт градуирован в `plan.md` A-12 и стоит там на 0.75. Причина понижения доказательная, а не модельная, и правка §10.3 v2.4 её не снимает: наблюдение получено тем же единственным прогоном `strings` по `global.ucas`, что и A-08, второго независимого метода до RF-01 нет, а класс I требует двух методов начиная с 0.80. Чтение `strings` по целому файлу вообще не имеет определённого адреса и длины, поэтому обязательное условие v2.4 к нему неприменимо по природе метода (`plan.md` A.6). Две конкурирующие догадки, ни одна не подтверждена: (а) особенность глобального name-пула; (б) нестандартная конфигурация cook. План прямо запрещает делать выводы до M2 |
| A-09 | Какие плагины реально **включены** в сборку, а не только staged | UNKNOWN (состав активных модулей — HYPOTHESIS, confidence 0.4) | M2 | CL-09 (ChunkDownloader), CR-08 (MetaSound), интерпретацию `research/packages/plugins.json`; exit criterion M2 (3) | `binary-analysis` (наличие модуля/кода в exe) + `global-ucas` (`/Script/<Plugin>`); `container-metadata` доказывает только факт staged-файла | Найдено 250 имён `*.uplugin` в индексе `MISERY-Windows.pak` (OBSERVED, confidence 0.75), в основном движковых, включая **ChunkDownloader**, EOSShared, MetaSound-связанные, `UniversalVoiceChatPro`. **Сверка с источником 2026-08-22 (LOG-0012):** здесь стояло 0.95; это число градуировано в `plan.md` A-09b и стоит там на 0.75 — один прогон `strings` по одному файлу, никем не перепроверенный. Частичная перепроверка 2026-08-22 (LOG-0012, чтение read-only своим кодом по последним 4 МиБ файла) нашла 125 различных имён `*.uplugin` в этом окне; это не опровергает 250 и не подтверждает их — окно меньше индекса, — но подтверждает, что имена в индексе читаемы. Пересчёт своим парсером — M2, `research/packages/plugins.json`. Правило плана: **staged ≠ enabled**, и это обязательно писать в артефакте явно |

### 3a. Открытые пункты Приложения A, не перечисленные в Приложении B

Перенесены сюда потому, что exit criteria M1/M2 требуют их закрытия, а Приложение B их не упоминает.
Это **не** новые вопросы — это зафиксированные в плане незакрытые интерпретации.

| ID | Вопрос | Статус | Milestone | Что блокирует | Oracle (§10.5) | Заметки / догадка |
|---|---|---|---|---|---|---|
| A-06 | Нужен **третий** независимый источник версии движка: exit criterion M1 (3) требует confidence ≥ 0.90 и **≥3** независимых источников, сейчас их два | IN PROGRESS | M1 | Exit criterion M1 (3); `research/unreal/engine-version.json` | `binary-analysis` (строки/константы в Shipping exe) + `container-metadata` (версии `.utoc`/`.pak` как косвенный источник) + `external-doc` (соответствие версий UE) | Имеющиеся два источника: UTF-16 строка `++UE5+Release-5.4-CL-35576357` в Shipping exe и `<EngineVersion>5.4.4-35576357+++UE5+Release-5.4</EngineVersion>` в `CrashContext.runtime-xml`. Второй источник обременён RISK-09 (crash-отчёты могут относиться к более старой сборке) — см. NEW-02. Текущая суммарная оценка: OBSERVED, confidence 0.92 |
| A-07p | Воспроизводятся ли **сами байты** заголовка `.utoc` при повторном чтении по фиксированным смещениям — примитивное измерение, без декодирования | ЗАКРЫТО частично: байты воспроизводятся, OBSERVED 0.99 | M2 | Ничего не блокирует: это вход для A-07i, а не самостоятельный exit criterion | `container-metadata` (чтение байтов по фиксированным смещениям; метод перезапущен и воспроизведён — второй проход зафиксирован в `RESEARCH_LOG.md` LOG-0007 п. 8) | **Отделено от A-07 2026-08-22** по правилу `plan.md` §10.3 v2.2 «смешанные утверждения обязаны разделяться». В прежней редакции обе половины — байты и их разбор — стояли в одной клетке под двумя уровнями сразу; прочитать, какая confidence относится к какой половине, было нельзя, и валидатор отвергал строку как нечитаемую (PARSE-MD). Здесь остаётся только примитивная половина. Источник воспроизведения — LOG-0007 п. 8: при втором ручном проходе байты по всем смещениям заголовка совпали с первым проходом, **включая** смещение 48 (`a0 e4 0c 00`). Расхождение возникло не в байтах, а в их разборе, поэтому оно записано в A-07i, а не здесь. **Обновление 2026-08-22 (LOG-0012), закрывает прежнюю оговорку этой строки о том, что третьего чтения не выполнялось:** байты по всем перечисленным смещениям прочитаны ещё раз read-only через `D:\Tools\venv-research\Scripts\python.exe` и воспроизведены, включая 4 байта по смещению 48 в `MISERY-Windows.utoc` = `a0 e4 0c 00`. Расхождений с прежними проходами нет. **Сверка с источником:** `plan.md` A-07 после правки §10.3 v2.4 стоит на OBSERVED 0.99 с oracle `container-metadata` и указанными у каждого значения смещением и длиной — та же оценка, что здесь, то есть расхождения между документами по этой строке больше нет. Обязательное условие v2.4 (смещение **и** длина в самом утверждении) выполнено и там, и здесь |
| A-07i | Подтверждены ли независимым oracle значения полей заголовков `.utoc`, **декодированные** вручную по публичному layout `FIoStoreTocHeader`, и вывод «основной контейнер зашифрован» | IN PROGRESS — INFERRED 0.85 (интерпретирующая половина прежней A-07; примитивная половина вынесена в A-07p). **Подвопрос о расхождении `DirectoryIndexSize` — ANSWERED 2026-08-22**, см. блок «ОТВЕЧЕНО» в заметках. Головной вопрос строки остаётся открытым: независимого oracle по-прежнему нет, и по контракту п. 3 слабый oracle вопрос не закрывает | M2 | Exit criterion M2 (1): «`containers.json` с полностью заполненными полями заголовков, **сверенными с независимым oracle**» (§17.3, C-12) | `container-metadata` (наш парсер `container_info.py`) + `external-doc` / проверенный сторонний инструмент как независимый oracle | Расхождение между нашим парсером и сторонним инструментом — находка для разбора, а не повод довериться одному (RISK-17). Вывод «основной контейнер зашифрован» тоже INFERRED (0.85), опирается на `ContainerFlags = 0x0A` и энтропийную проверку. **Новое наблюдение 2026-08-22 (повторный ручной декод, второй проход):** повторное чтение заголовка `MISERY-Windows.utoc` воспроизводит все поля A-07, **кроме одного** — `DirectoryIndexSize` по смещению 48 равен `0x000CE4A0` = **844 960**, тогда как `plan.md` A-07 указывает **845 472** (`0x000CE6A0`). Байты по этому смещению: `a0 e4 0c 00`. Похоже на ошибку переписывания при первом ручном декоде, но какое из двух чтений верно — вопрос к владельцу `plan.md` A-07, а не к этому реестру. Значение важное: `DirectoryIndexSize` определяет, где кончается directory index и начинается тело TOC. Это ровно тот класс ошибки, под который A-07 градуирован INFERRED 0.85, а не OBSERVED: байты читаются надёжно, а их разбор вручную — нет. Подтверждает необходимость независимого oracle, а не снимает её. **ОТВЕЧЕНО 2026-08-22 — только подвопрос о расхождении, не вся строка.** Ответ: `DirectoryIndexSize` для `MISERY-Windows.utoc` равен **844 960**. Что ответило: `plan.md` §Приложение A, строка A-07 (байты по смещению 48, градуированы там; здесь их уровень не пересказывается — §10.1), строка A-07i (десятичное значение 844 960) и раздел A.3, где зафиксировано, что в recon-редакции была арифметическая ошибка при переводе `0x000CE4A0`, а не расхождение чтений. Байтовая половина этого ответа живёт в строке A-07p выше и в `plan.md` A-07; пятое прочтение read-only 2026-08-22 зафиксировано в `RESEARCH_LOG.md` LOG-0011. Достигнутый по подвопросу уровень — INFERRED, confidence 0.85, oracle `container-metadata` + `external-doc`: сам ответ есть перевод байт в имя поля и число, а такой перевод опирается на публичную раскладку `FIoStoreTocHeader`, то есть выше INFERRED не поднимается ни при каком числе прочтений. Прежняя формулировка подвопроса («какое из двух чтений верно — вопрос к владельцу `plan.md` A-07») сохранена выше как история по контракту п. 1. Число 845 472 с этого момента не цитируется как факт нигде, кроме записей об исправленной ошибке. **Что осталось открытым в этой строке:** независимый oracle для декодированных полей (exit criterion M2 (1), C-12) — наш собственный повторный разбор независимым oracle не является, сколько бы проходов он ни насчитал |
| A-13 | Какова природа PE link timestamp `1918640348` (номинально 2030-10-19) у `MISERY-Win64-Shipping.exe` | UNKNOWN (интерпретация; факт OBSERVED 0.85 — сверено с `plan.md` A-13 2026-08-22, LOG-0012; здесь стояло 0.99) | M1 | Возможность использовать `pe.timestamp` как признак идентичности сборки (§3.1, §16.1) | `binary-analysis` (Rich header, сопоставление с timestamp второго exe 1784645631) | Почему 0.85: числа держатся на одиночных ручных чтениях recon, размеры секций никем не перепроверялись. Правка §10.3 v2.4 понижения не снимает — клетка называет поле `TimeDateStamp` и секцию `.msvcjmc`, то есть опирается на внешнюю раскладку PE, а не на байты по указанному смещению (`plan.md` A.6, случай (c)). **HYPOTHESIS «детерминированный / хешированный timestamp», confidence 0.6.** До ответа `pe.timestamp` не используется как элемент версионной идентичности — для этого есть `build_key` (§3.2) |

---

## 4. Контейнеры и контент

| ID | Вопрос | Статус | Milestone | Что блокирует | Oracle (§10.5) | Заметки / догадка |
|---|---|---|---|---|---|---|
| Q-2.2 | Существует ли `MISERY/Config/*.ini` на диске | **ANSWERED** — нет | M0 (перепроверка штатным инструментом) | Ничего | `filesystem` → подтверждается `research/builds/<build-id>/install-inventory.json` | Recon: конфиги лежат внутри контейнеров. Следствие, важное для §13: `%LOCALAPPDATA%\MISERY\Saved\Config\Windows\Engine.ini` — **единственная незапакованная точка влияния на конфиг** (кандидат bootstrap B-6). Запись остаётся как история |
| Q-2.3 | Читает ли сборка дополнительные контейнеры из `Paks/` автоматически (`-pak` / авто-обнаружение) | UNKNOWN | M5a | Q-8.5 (нужен ли уровень 2 вообще) и тем самым объём §8; шаг 1 «discovery» цепочки §14.1 | `runtime-reflection` (I-14). `container-metadata` этого **не** доказывает. Дополнительно `binary-analysis` — точки вызова mount API (§7.4 п.5) | Оснований для догадки нет. Это первый вопрос, который стоит закрывать в M5a: от него зависит, останется ли M5a полностью на уровне 1 |
| Q-2.4 | Поддерживает ли сборка `Paks/*_P.pak` (patch pak) и chunk-приоритеты | UNKNOWN | M5a | CL-05 (порядок/приоритет), дизайн раскладки мод-контейнеров | `binary-analysis` + `runtime-reflection` (I-14) | Оснований для догадки нет |
| Q-2.5 | Меняется ли `installdir` между версиями и языками | UNKNOWN, низкий приоритет | M0/M1 (полный ответ требует второй сборки, RISK-06) | Устойчивость `find_misery.py` / `find-misery.ps1` к нестандартным установкам. Критичного не блокирует | `steam-metadata` (`libraryfolders.vdf`, `appmanifest_2119830.acf`) | Сейчас `installdir "MISERY"` (OBSERVED). Discovery не должен полагаться на это значение как на константу |
| CL-01 | Какие API mount доступны в этой сборке: pak? IoStore? оба? | UNKNOWN | M5a (кандидаты — статически в M2s) | Выбор Tier и формата тестового контейнера (§14.7); всю mounting-часть §14 | `binary-analysis` (`FPakPlatformFile::Mount`, `FIoDispatcher`, `FIoStoreEnvironment`) + `runtime-reflection` (I-14) | Косвенный признак: `MISERY-Windows.pak` присутствует рядом с IoStore-контейнерами, то есть legacy pak-путь в сборке присутствует. То, что он реально монтируется в runtime — INFERRED, confidence 0.8, **не факт** (см. NEW-04) |
| CL-02 | Требует ли IoStore-mount ключа шифрования игры для **нашего** контейнера, если наш контейнер не зашифрован | UNKNOWN — гейт для применимости D-02 | M5a | Применимость всего IoStore-пути: если ключ обязателен, при принятом D-02 остаётся только pak-путь | `binary-analysis` + `runtime-reflection` (I-14) | Оснований для догадки нет. Отрицательный ответ не отменяет D-02 — он сужает набор доступных путей |
| CL-03 | Можно ли смонтировать **директорию** (loose files) вместо контейнера; поддерживает ли Shipping-сборка `-pak` / loose file fallback | UNKNOWN | M5a | Стоимость всего content pipeline; связано с Q-8.5 | `binary-analysis` + `runtime-reflection` (I-14) | Если да — резко упрощает всё. Оснований для догадки нет |
| CL-04 | Какой mount point использовать для мод-контента, чтобы не пересекаться с `/Game` | UNKNOWN | M5a | Дизайн mount root (в E-1 предполагается вид `/MiseryMods/Test/...`); настраиваемый mount point в CT-01 | `runtime-reflection` (I-14) + `container-metadata` (mount point нашего pak) + `external-doc` | Известно: mount point `MISERY-Windows.pak` = `../../../` (OBSERVED). Что примет сборка для чужого контейнера — UNKNOWN |
| CL-05 | Порядок и приоритет mount, поведение при коллизии имён | UNKNOWN | M5a (частично E-6 в M5b) | E-6; формулировку правила «наш инструмент не становится механизмом замены оригинального контента по умолчанию» | `binary-analysis` + `runtime-reflection` (I-14) | Оснований для догадки нет |
| CL-06 | Принимает ли сборка контейнеры, cooked другой минорной версией UE | UNKNOWN | M5a → M5b (E-7) | E-7; выбор версии движка при положительном D-06; часть RISK-15 | эксперимент + `container-metadata`; `binary-analysis` для проверок версий в коде | Правило плана: использовать ровно версию сборки (5.4.4), а не «близкую» |
| CL-07 | Есть ли проверка подписи / целостности контейнеров (pak signing) | UNKNOWN | **M2** | Осуществимость Tier A вообще: если подпись обязательна, наш неподписанный pak будет отвергнут и весь дешёвый путь §14.7 рушится. Exit criterion M2 (4) | `container-metadata` (наличие `.sig`, поля подписи) + `binary-analysis` (код проверки) | Слабый негативный признак: файлов `.sig` в recon не замечено, но директория `.utoc` не парсилась. Догадкой это считать нельзя |
| CL-08 | В какой момент запуска mount ещё возможен, а когда уже поздно | UNKNOWN | M5a | **§13 целиком**: даёт жёсткое требование «момент инициализации» в матрице §13.3 и может исключить часть кандидатов B-1..B-7. Exit criterion M5a (2), вход в M7 | `binary-analysis` (порядок инициализации) + `runtime-reflection` (I-14, наблюдение в разные моменты запуска) | Один из самых ценных дешёвых ответов: закрывается Tier A, без UE и без cooked-контента |
| CL-09 | Есть ли у сборки `ChunkDownloader` / штатная поддержка внешних контейнеров | UNKNOWN | M5a | Наличие штатного пути сильно упростило бы §13 и §14; отсутствие — не блокирует, но повышает стоимость | `binary-analysis` + `global-ucas` (`/Script/ChunkDownloader`). Наличие `.uplugin` (`container-metadata`) **не** доказывает включённость модуля | `ChunkDownloader.uplugin` присутствует среди staged-файлов (OBSERVED, A-09). **HYPOTHESIS «модуль включён в сборку», confidence 0.4** — это догадка, зависящая от A-09 |
| **CR-01** | Как MISERY получает список предметов и прочих content definitions | UNKNOWN | M3 (1-й проход) → M5b (подтверждение) | **ГЕЙТ**, RISK-05 — см. таблицу гейтов | `runtime-reflection` + `asset-registry`, желательно + `binary-analysis`; **одного oracle недостаточно** | Первый проход **не требует** ни cooker-а, ни экспериментов: игровые registry/manager-классы видны в reflection map сразу. Целевая confidence: 0.5–0.7 в M3, ≥0.8 в M5b |
| CR-02 | Можно ли расширить этот список в runtime **без изменения оригинальных данных** | UNKNOWN | M5b | Вердикт по registration; саму возможность аддитивного моддинга (C-01) | `runtime-reflection` + `binary-analysis` | Зависит от формы ответа на CR-01. Оснований для догадки нет |
| CR-03 | Использует ли игра `UAssetManager` и `PrimaryAssetLabel` / `AssetBundles` | UNKNOWN | M5b (первые признаки уже в M3) | Форму ответа на CR-01 и CR-02 | `runtime-reflection` + `asset-registry` | Оснований для догадки нет |
| CR-04 | Как регистрируются BP-классы; будет ли внешний cooked BP-класс виден через reflection | UNKNOWN | M5b (E-3a) → M5c (E-3b) | E-3a и E-3b | `runtime-reflection` + эксперимент | Частичное приближение возможно статически (SP-1): если загрузка пакета в UE идёт общим путём независимо от обслуживающего контейнера, E-3a получает confidence ~0.7 **до** установки UE. Это приближение, а не ответ |
| CR-05 | Есть ли в игре собственная система «регистрации контента» (subsystem, registry, modular gameplay) | UNKNOWN | M3 (вместе с 1-м проходом CR-01) → M5b | CR-01 | `runtime-reflection` + `binary-analysis` | Косвенный признак: в `/Script/MISERY` уже видны имена вида `MiseryFocusSubsystem` (OBSERVED, A-08), то есть свои subsystem-ы в игре есть. Что среди них есть content registry — догадкой пока не является: имя в `global.ucas` не доказывает роль класса |
| CR-06 | Как это работает в co-op: должен ли мод-контент быть у всех участников | UNKNOWN | M5b / M8 (связан с N-09) | Требования безопасности к будущему Runtime (RISK-10); дизайн распространения мод-контента | `runtime-reflection` (I-12) + эксперимент в приватной сессии (D-05) + `binary-analysis` | Оснований для догадки нет |
| CR-07 | Требуют ли `Material` / Niagara отдельных cooked shader-артефактов (shader library, `.ushaderbytecode`) и отдельной runtime-регистрации — даже если Texture/StaticMesh уже работают | UNKNOWN | M5b (E-2b, E-4) | Exit criterion M5b (4); ответ на вопрос «какой вообще контент возможен» | эксперимент + `container-metadata` (наличие shader library) + `binary-analysis` | RISK-16. Обязательное правило фиксации: «материал загрузился, но рендерится как default/чёрный» записывается как **успех загрузки при провале shader-пути**, а не как общий провал. Результаты E-2b/E-4 не смешивать с E-1/E-2a |
| CR-08 | Требует ли audio отдельной цепочки (MetaSound-регистрация, audio banks) | UNKNOWN | M5b (E-5) | Exit criterion M5b (5) | эксперимент + `binary-analysis` + `global-ucas` (`/Script/MetaSound*`) | MetaSound-плагин присутствует среди staged (A-09) — это делает отдельную цепочку вероятной, но включённость модуля сама UNKNOWN (см. A-09) |
| CT-05 | Реализуем ли Tier B на практике — ручной legacy-сериализатор `tools/content/mini_cook.py`, производящий минимальный cooked package (`.uasset` + `.uexp`) | UNKNOWN | M5b | Шаг 4 «load» цепочки §14.1 и E-1 **без** UE. При отрицательном ответе E-1 ждёт Tier C/D | вне матрицы §10.5 (инженерная реализуемость); результат проверяется `container-metadata` и экспериментом | Основание для оптимизма: в UE 5.4 при включённом IoStore пакеты внутри `.pak` продолжают использовать legacy-раскладку, которая существенно проще Zen. Явное предупреждение плана: Tier B **не масштабируется** на меши, материалы и особенно Blueprint — пытаться не надо. Exit criterion допускает оба исхода: пакет принят **или** дана диагностируемая ошибка |
| D-06 | Ставить ли локально UE 5.4.x (минимальная установка ~40 ГБ) | UNKNOWN — открыто **по замыслу** | по триггеру §14.7 | Tier C, а значит E-2a, E-2b, E-3a, E-3b, E-3c, E-4, E-5, E-7, весь §14A и M5c | не применим — решение владельца по четырём условиям триггера | Условия триггера, все одновременно: (1) Tier A дал «внешний контейнер монтируется»; (2) известно требование CL-08; (3) SP-1 не дал уверенного отрицательного ответа ни по E-3a, ни по CK-04; (4) на диске ≥60 ГБ с запасом. На 2026-08-22 на `D:` ~98 ГБ — условие 4 выполнимо, условия 1–3 нет. Резервный путь при отказе — Tier D (cook вне этой машины) |

### 4a. Вердикты экспериментов E-1..E-7 (§5.3)

Приложение B перечисляет из этой группы только E-3b. Остальные внесены потому, что exit criteria
M5b/M5c требуют для **каждого** эксперимента записи `pass` / `fail` / `blocked` с указанием шага
цепочки §14.1, использованного Tier и сохранённого артефакта.

| ID | Вопрос (вердикт) | Статус | Milestone | Что блокирует | Oracle (§10.5) | Заметки / догадка |
|---|---|---|---|---|---|---|
| E-1 | Монтируется и загружается ли простейший cooked `Texture2D` из внешнего контейнера | UNKNOWN | M5b (Tier B или C) | Exit criteria M5b; базовая проверка шага 4 «load» | эксперимент + `runtime-reflection` | Критерий провала различать явно: ошибка mount / ошибка десериализации / crash — это три разных результата |
| E-2a | Загружается и **рендерится** ли StaticMesh с уже существующим (движковым) материалом | UNKNOWN | M5b (Tier C) | Exit criteria M5b | эксперимент + `runtime-reflection` | «Загрузился, но не рендерится» — отдельная запись, это другой класс проблемы (RISK-16) |
| E-2b | Требует ли **собственный** `Material` собственных cooked shader-артефактов и их регистрации | UNKNOWN | M5b (Tier C) | CR-07; exit criterion M5b (4) | эксперимент + `container-metadata` + `binary-analysis` | RISK-16. Рендер как default/чёрный = успех загрузки при провале shader-пути |
| E-3a | Регистрируется ли в reflection и спавнится ли cooked BP, унаследованный от **движкового** `AActor` | UNKNOWN | M5b (Tier C) | Необходимое условие для E-3b; при `fail` §14 закрывается отрицательно | эксперимент + `runtime-reflection` | Статическое приближение — SP-1 (CT-04), confidence ~0.7, **не** замена эксперименту. Прежнее обозначение статического прокси в v1 было `E-3a`; теперь это `SP-1` — старые ссылки обязаны обновляться, а не сосуществовать |
| **E-3b** | Загружается ли cooked BP, унаследованный от native-класса `/Script/MISERY` | UNKNOWN | M5c (MK-3) | **ГЛАВНЫЙ ГЕЙТ ПРОЕКТА** — см. таблицу гейтов | эксперимент + `runtime-reflection` | Достаточное условие для утверждения «meaningful custom content возможен» |
| E-3c | Возможно ли наследование от **Blueprint**-класса игры (не native) | UNKNOWN | M5c | Exit criterion M5c (4) | анализ + вердикт (`binary-analysis` + `asset-registry`) | **Ожидание плана (догадка, не результат): вероятный вердикт `blocked by D-02`**, потому что редактор должен видеть родительский BP-asset, а он лежит в зашифрованном контейнере (RISK-02, CK-09). Зафиксировать честно, а не обходить |
| E-4 | Воспроизводится ли Niagara System из внешнего контейнера | UNKNOWN | M5b (Tier C) | CR-07; exit criterion M5b (4) | эксперимент + `binary-analysis` | Как и в E-2b, различать «загрузился» и «отрендерился» (RISK-16) |
| E-5 | Проигрывается ли звуковой asset (`SoundWave` / MetaSound) из внешнего контейнера | UNKNOWN | M5b (Tier C) | CR-08; exit criterion M5b (5) | эксперимент + `binary-analysis` | Возможна отдельная цепочка требований из-за MetaSound |
| E-6 | Что происходит при совпадении package name нашего контейнера с оригинальным | UNKNOWN | M5b | CL-05; правила приоритета | эксперимент + `runtime-reflection` | Явное ограничение плана: этот эксперимент **не должен** становиться механизмом замены оригинального контента по умолчанию. Цель — только понять правила приоритета. В v1 обозначался `E-5` |
| E-7 | Отклоняет ли сборка контейнер, приготовленный другим минором UE | UNKNOWN | M5b | CL-06; часть RISK-15 | эксперимент + `container-metadata` | Требует двух версий UE, то есть зависит от D-06. В v1 обозначался `E-6` |

---

## 5. Mod Kit (§14A)

Полный набор CK-01..CK-10. CK-01 и CK-04 дополнительно вынесены в таблицу гейтов.

| ID | Вопрос | Статус | Milestone | Что блокирует | Oracle (§10.5) | Заметки / догадка |
|---|---|---|---|---|---|---|
| **CK-01** | Использует ли сборка unversioned property serialization в cooked-пакетах | UNKNOWN | M2s | **ГЕЙТ для §14A** — см. таблицу гейтов. Exit criterion M5c (1) требует confidence ≥0.8 | `binary-analysis` + `container-metadata` | Отвечается **до** установки UE. Самый опасный исход RISK-15 — «пакет принят и прочитан неверно» |
| CK-02 | Можно ли в editor-проекте создать модуль с именем ровно `MISERY`, чтобы получить package-путь `/Script/MISERY` | UNKNOWN | M5c (MK-1) | MK-1, а значит весь §14A: без совпадения package-пути import в нашем пакете не сойдётся с игрой | `external-doc` + практическая проверка. Здесь `external-doc` достаточно, потому что утверждение касается **нашего** проекта, а не сборки игры | **Догадка** (основание — имя модуля в UE задаётся произвольно): технически возможно. Не проверено; проверка требует установленного UE (D-06) |
| **CK-03** | Достаточно ли reflection-данных (I-04..I-06) для генерации компилируемого stub-а: имена, типы, флаги, **порядок**, размеры, зависимости `USTRUCT`/`UENUM` | UNKNOWN | M5c (данные приходят из M3) | MK-1. При «нет» §14A закрывается на первой стадии. Exit criterion M5c (1) требует confidence ≥0.8 | `runtime-reflection` | Прямо диктует требование к M3: exit criterion M3 (1) обязывает выгружать **порядок и размеры** свойств, а не только имена. Оснований для догадки нет |
| **CK-04** | Офсеты свойств дочернего BP-класса запекаются при cook или пересчитываются при `Link()` | UNKNOWN | M2s (SP-1 / CT-04) | **РЕШАЮЩИЙ для §14A** — см. таблицу гейтов. Также входит в условие 3 триггера §14.7 | `binary-analysis` | Артефакт: `research/packages/sp1-static-proxy.md`. Ответ получается без cook и без UE — поэтому он идёт до решения о 40 ГБ |
| CK-05 | Какие custom versions регистрируют модули игры и требуются ли они при загрузке пакета | UNKNOWN | M5c | MK-2 (cook); часть RISK-15 | `binary-analysis` | Оснований для догадки нет |
| CK-06 | Какие настройки cook у игры (IoStore, компрессия, шифрование, платформа, форматы шейдеров) и воспроизводимы ли они в нашем проекте | UNKNOWN | M5c (задача CT-07) | MK-2; часть RISK-15 | `container-metadata` (+ `binary-analysis`) | Частично уже видно: `ContainerFlags = 0x0A` = `Encrypted \| Indexed` **без** `Compressed` у основного контейнера, `CompressionBlockSize` 65536, `PartitionCount` 1 — но это INFERRED (A-07, 0.85) и требует сверки независимым oracle |
| CK-07 | Каков транзитивный набор типов, который надо сгенерировать для одного осмысленного целевого класса | UNKNOWN | M5c | Объём работы MK-1 и оценки MK-5. Гейтом не является | `runtime-reflection` | Оснований для количественной догадки нет — зависит от того, какой класс выбран целевым |
| CK-08 | Проверяет ли игра происхождение / целостность загружаемых классов | UNKNOWN | M5c | E-3b — при положительном ответе появляется дополнительный барьер; связано с CL-07 | `binary-analysis` | Оснований для догадки нет |
| CK-09 | Возможен ли E-3c (наследование от Blueprint-класса игры) при принятом D-02 | UNKNOWN | M5c | E-3c; exit criterion M5c (4) | анализ + вердикт (`binary-analysis` + `asset-registry`) | **Ожидание плана, помеченное как вероятное, а не установленное: `blocked`.** Причина — родительский asset лежит в зашифрованном контейнере, а редактор должен его видеть (RISK-02). Записывать честно как `blocked by D-02`, а не как технический провал |
| CK-10 | Масштабируется ли генерация stub-ов на весь модуль `/Script/MISERY` или только на выборочные классы | UNKNOWN, низкий приоритет | M5c (после MK-3) | Только оценку будущего публичного mod kit. В Phase 1 MK-5 — это **оценка**, а не производство | практика + `runtime-reflection` | Известен масштаб входа: в `global.ucas` найдено 394 уникальных `/Script/<Module>`, среди них `/Script/MISERY`; сколько в нём классов — UNKNOWN до M3 |

---

## 6. Bootstrap, сеть, версии

### 6.1 Bootstrap

| ID | Вопрос | Статус | Milestone | Что блокирует | Oracle (§10.5) | Заметки / догадка |
|---|---|---|---|---|---|---|
| Q-2.1 | Какой exe реально стартует Steam — корневой shim или сразу `MISERY-Win64-Shipping.exe` | IN PROGRESS (частично отвечен) | M7 (наблюдение возможно и раньше) | Пространство кандидатов §13.2 — напрямую B-1, B-3, B-7; матрицу §13.3 | `steam-metadata` (что Steam записал) + `binary-analysis` (доказывает только содержимое shim) + наблюдение дерева процессов при обычном запуске — **для последнего значения в §10.5 нет и после правки v2.1**, см. NEW-01 | Отвечена **часть**, и она сама распадается на две половины, градуированные в `plan.md` по-разному — здесь они разведены 2026-08-22 (LOG-0012), потому что прежде обе стояли под одним числом 0.95, которого нет ни у одной из них. Примитивная половина: корневой `MISERY.exe` имеет размер 422 400 B, и в его UTF-16 строках присутствуют литералы `BootstrapPackagedGame-Win64-Shipping.exe`, `MISERY\Binaries\Win64\MISERY-Win64-Shipping.exe`, `Engine\Extras\Redist\en-us\UEPrereqSetup_x64.exe` (OBSERVED, confidence 0.85 — градуировано в `plan.md` A-04). Интерпретирующая половина: этот exe является UE-шимом `BootstrapPackagedGame`, то есть двухступенчатый запуск в игре уже существует (INFERRED, confidence 0.85 — градуировано в `plan.md` A-04i; уровень INFERRED делает её классом I безусловно). Не отвечено: что именно вызывает Steam. **Q-2.1 и Q-13.0 — две формулировки одного факта и должны закрываться одним артефактом** |
| Q-13.0 | Что Steam указывает как launch executable для AppID 2119830 | UNKNOWN | M7 | **§13 целиком**: какие варианты bootstrap вообще применимы; решение D-03 | `steam-metadata` + наблюдение дерева процессов (для второго значения в §10.5 нет — NEW-01); дополнительно проверка того, что видит Steam-оверлей / `steam_appid` | См. Q-2.1. Оснований для догадки нет: `appmanifest_2119830.acf` этого поля не содержит |
| D-03 | Какой механизм bootstrap выбрать из B-1..B-7 | UNKNOWN — открыто **по замыслу** | M7 | Exit criterion M7; архитектуру Phase 2 | не применим — решение по заполненной матрице §13.3 плюс жёсткое требование CL-08 | Выбирать заранее запрещено (§0.3). Матрица §13.3 сейчас UNKNOWN во всех ячейках. Обязательные к заполнению без UNKNOWN строки: «требует изменения оригинальных файлов», «момент инициализации», «выживаемость после обновления». Recon-факт, сужающий поиск: двухступенчатый запуск в игре **уже есть** (A-04) — цепочку не надо изобретать, надо понять, куда в неё можно встроиться без изменения оригинальных файлов (C-01) |

### 6.2 Networking (§12.1)

Все девять требуют минимум одной сессии; по D-05 — **только** полностью контролируемые private sessions.

| ID | Вопрос | Статус | Milestone | Что блокирует | Oracle (§10.5) | Заметки / догадка |
|---|---|---|---|---|---|---|
| N-01 | Модель сети: listen server (host — игрок) / dedicated / P2P через Steam / EOS | UNKNOWN | M8, требуется confidence ≥0.8 | Exit criterion M8; весь дизайн multiplayer-безопасных модов | `binary-analysis` + `runtime-reflection` (I-12) в контролируемой сессии | В сборке присутствуют одновременно `steam_api64.dll`, `EOSSDK-Win64-Shipping.dll` и `msquic` — **роль каждого UNKNOWN**. Наличие библиотеки не доказывает, что она используется (та же логика, что «staged ≠ enabled») |
| N-02 | Как определить authority у объекта в runtime (`GetNetMode`, `HasAuthority`, `Role`/`RemoteRole`) | UNKNOWN | M8, ≥0.8 | Exit criterion M8; любые правила «что мод может делать в роли client / host» | `runtime-reflection` (I-12) | Оснований для догадки нет |
| N-03 | Какие акторы реплицируются; какие свойства помечены replicated | UNKNOWN | M8 | `research/reflection/<build_key>/replicated-properties.jsonl` | `runtime-reflection` (флаги `FProperty`) + `binary-analysis` (generated `GetLifetimeReplicatedProps`) | Дёшево: флаги приходят тем же дампом, что M3 |
| N-04 | Какие RPC существуют (`NetMulticast`, `Server`, `Client`) и на каких классах | UNKNOWN | M8 | `research/reflection/<build_key>/rpcs.jsonl` | `runtime-reflection` (флаги `UFunction`) | Дёшево, из того же дампа M3 |
| N-05 | Ownership: как определить, что актор принадлежит локальному игроку | UNKNOWN | M8, ≥0.8 | Exit criterion M8 | `runtime-reflection` (I-09, I-12) | Зависит от M4 |
| N-06 | Local vs remote players: как перечислить всех игроков и отличить локального | UNKNOWN | M8, ≥0.8 | Exit criterion M8 | `runtime-reflection` (I-09) | Зависит от M4 |
| N-07 | Что делает сборка при рассинхронизации или получении невалидных данных | UNKNOWN | M8 | Требования безопасности к будущему Runtime (RISK-10) | `binary-analysis` + наблюдение в контролируемой сессии | Оснований для догадки нет |
| N-08 | Есть ли серверная валидация действий клиента | UNKNOWN | M8 | Границы допустимого для модов | `binary-analysis` + `runtime-reflection` | §12.3: обходы серверной валидации **не разрабатываются**. Вопрос исследуется, чтобы знать границы, а не чтобы их обходить |
| N-09 | Как игра обрабатывает несовпадение версий контента между хостом и клиентом (у хоста мод есть, у клиента нет) | UNKNOWN | M8, ≥0.8; эксперимент — после M6 | §14 в co-op-контексте; CR-06; RISK-10 (порча сейвов у пользователя) | эксперимент в приватной сессии (D-05) + `runtime-reflection` + `binary-analysis` | Помечен планом как **критически важный** для §14. Оснований для догадки нет |

### 6.3 Version compatibility (§16.3)

| ID | Вопрос | Статус | Milestone | Что блокирует | Oracle (§10.5) | Заметки / догадка |
|---|---|---|---|---|---|---|
| VC-01 | Какие идентификаторы фактически стабильны в этой игре между сборками | UNKNOWN — **блокировано RISK-06** | недостижим в Phase 1 при одной сборке | Обоснованность §16.1 (какие идентификаторы допустимы в публичном API). Phase 1 не блокирует, но ограничивает её выводы | тот же oracle, что у сравниваемых дампов (`runtime-reflection`, `binary-analysis`), но **требует второй сборки** | Доступна одна сборка (`steam_buildid 24826585`). До появления второй все утверждения о стабильности остаются HYPOTHESIS — §16.4 требует записать это честно. Митигация: `tools/kb/diff_builds.py` пишется **сейчас**, применяется при первом патче |
| VC-02 | Насколько устойчивы сигнатуры функций между сборками | UNKNOWN — блокировано RISK-06; частичная оценка возможна | частично M2s, полный ответ — после второй сборки | Правило §16.1 «сигнатуры только внутри Runtime, не в публичном API» | `binary-analysis` (S-06 `sigmake.py`, S-07 `sigscan.py`) | Частичный путь, предусмотренный планом: сравнить `MISERY-Win64-Shipping.exe` и второй `MISERY.exe` (D-04) как две разные компиляции. Любой вывод оттуда обязан быть перепроверен на Shipping-бинарнике |
| VC-03 | Меняются ли имена BP-классов и свойств между патчами | UNKNOWN — блокировано RISK-06 | недостижим в Phase 1 | Допустимость `asset-path` и имён BP в публичном API | `asset-registry` / `runtime-reflection` на двух сборках | Оснований для догадки нет |
| VC-04 | Как автоматически детектировать «сборка изменилась» | **ANSWERED** (проектный ответ) | реализация — M1 (F-03) | Ничего | не применим — проектное решение | Ответ §3.2: `build_key = sha256(MISERY-Win64-Shipping.exe)` + `content_key = sha256(concat(sha256 всех .utoc))`, реестр `research/builds/index.json`. Это позволяет различать «патч только контента» и «патч кода». Открыта только реализация, не вопрос. Артефакт при закрытии: `research/builds/index.json` + `fingerprint.json` |
| VC-05 | Как хранить bindings, чтобы добавление новой сборки не требовало правки кода | UNKNOWN (дизайн) | M9, документируется в `docs/version-compatibility.md` | Форму `bindings/<build_key>.json` и тем самым архитектуру Phase 2 | не применим — проектное решение | Направление задано §16.2: декларативный формат, моды видят только stable API. Конкретной схемы нет |

---

## 7. Сводка по состоянию на 2026-08-22

Всего записей утверждённого набора: **75** (гейтовые вопросы посчитаны один раз, по своей
тематической таблице; верхняя таблица гейтов — повторное представление, а не отдельные записи).

| Статус | Количество | Кто именно |
|---|---|---|
| UNKNOWN | 69 | все остальные |
| IN PROGRESS | 3 | Q-2.1 (отвечена часть), A-06 (нужен третий источник), **A-07i** (нужен независимый oracle; подвопрос о расхождении `DirectoryIndexSize` отвечен 2026-08-22, головной вопрос — нет). Прежнее имя в этой таблице было `A-07`; строка разделена 2026-08-22 на A-07p и A-07i, и IN PROGRESS относится к A-07i — A-07p закрыта частично и exit criterion-ом не является |
| ANSWERED | 3 | Q-2.2 (конфигов на диске нет), Q-8.4 (проектное решение §8.6), VC-04 (`build_key` + `content_key`, §3.2) |

Отдельно, чтобы отвеченное не пряталось внутри открытой строки: **подвопрос A-07i о расхождении
`DirectoryIndexSize` 844 960 против 845 472 — ANSWERED 2026-08-22** в пользу 844 960, со ссылкой на
`plan.md` A-07 / A-07i / A.3 и на `RESEARCH_LOG.md` LOG-0011. Он не считается отдельной записью
реестра (ID не заводится: он не из плана, а контракт п. 7 запрещает плодить вторые записи), поэтому
итог «75 записей» не меняется, а статус строки A-07i остаётся IN PROGRESS по головному вопросу.

Из них гейтов — 5 (Q-8.3, CK-01, CK-04, CR-01, E-3b). Гейты, достижимые **немедленно доступным**
инструментарием: Q-8.3 (и рядом Q-8.2, S-10) — read-only разбор exe; CK-01 и CK-04 — статический
анализ в M2s. Ни один из них не требует ни установки UE, ни запуска игры.

---

## 8. Вновь выявленное — НЕ утверждено планом

Ниже — пробелы, замеченные при составлении этого реестра. Они **не** входят в утверждённый набор
Приложения B, вынесены отдельно намеренно и до решения владельца не считаются частью плана.
ID с префиксом `NEW-` присвоены только для ссылок внутри этого файла.

| ID | Вопрос | Почему это пробел, а не придирка | Что предлагается |
|---|---|---|---|
| **NEW-01** | **ЗАКРЫТО ЧАСТИЧНО (2026-08-22).** Матрица oracle §10.5 не содержала класса источника для утверждений уровня ОС и Steam: дерево процессов, `appmanifest`, launch options, инвентарь файлов установки | На такие источники **уже опирались** несколько вопросов плана: Q-13.0 и Q-2.1 (что запускает Steam), Q-2.5 (`installdir`), частично Q-8.3 (отсутствие файлов анти-чита), A-02 (данные `appmanifest`). Задача EV-04 требует, чтобы линтер отклонял запись, чей тип утверждения не подтверждён нужным oracle — при отсутствии подходящего значения `oracle` эти записи либо не прошли бы валидацию, либо были бы помечены неверным oracle, что прямо подрывает смысл §10.5. Масштаб оказался больше, чем виделся при составлении реестра: только в `research/repo-audit.md` таких строк около пятидесяти | **Что сделано:** `plan.md` §10.5 (правка v2.1 от 2026-08-22) добавил не один сборный oracle, а **три раздельных** — `filesystem`, `steam-metadata`, `vcs-history` — каждый со своей строкой «что НЕ доказывает» и своими строками в матрице. Раздельные оказались лучше предлагавшегося `host-observation`: они разводят «что лежит на диске» и «что Steam записал», а расхождение между этими двумя утверждениями само является находкой (RA-09) и при одном сборном oracle было бы невыразимо. **Что осталось открытым (это и есть остаток NEW-01, статус UNKNOWN):** (1) наблюдение **дерева процессов** и launch options Steam при запуске игры — Q-2.1, Q-13.0; (2) **содержимое** crash-отчётов `CrashContext.runtime-xml` и логов движка: артефакт произведён runtime игры, но не является нашим наблюдением runtime, и `external-doc` здесь ошибка (исправлено в `repo-audit.md` §3). Оба класса нужны раньше M7, поэтому решение требуется до закрытия M1. Предлагается: либо десятый oracle с рабочим именем `host-observation`, строго ограниченный процессами и порождёнными игрой отчётами, либо решение «в knowledge base не заносится» — в `decisions.md`, а не молчанием |
| **NEW-02** | Нет ID-вопроса «относятся ли crash-отчёты в `%LOCALAPPDATA%\MISERY\Saved\Crashes` к текущей сборке» | RISK-09 фиксирует риск и §4.2 предписывает митигацию (mtime + обязательное совпадение с текущим exe), но открытого вопроса с ID нет — а от него зависит, остаётся ли `CrashContext.runtime-xml` вторым независимым источником версии движка. Если отчёты от старой сборки, доказательная база A-06 сокращается до одного источника, и exit criterion M1 (3) (≥3 источника) становится тяжелее | Либо завести отдельный ID в §4, либо явно указать в §4.2, что этот вопрос закрывается внутри A-06, и тогда он остаётся здесь как часть записи A-06 |
| **NEW-03** | Нет вопроса «можно ли в этой Shipping-сборке включить лог движка (`-log`, `Engine.ini`, `LogFile`) и что он покажет» | `Saved\Logs\` пуст (A-10, OBSERVED), и план объясняет это как ожидаемое для Shipping без `-log`, но **не спрашивает, включается ли лог вообще**. Если включается, это дешёвый почти-независимый источник наблюдения для CL-01..CL-09 и особенно CL-08: факт монтирования контейнера и момент монтирования могут быть видны в логе **без** чтения памяти процесса и без эскалации на уровень 2, что напрямую влияет на Q-8.5 | Завести вопрос в §14.3 или §8.2 и проверить его в M5a до реализации сценария наблюдения CT-03. Замечание о границах: лог — производный источник. После правки v2.1 §10.5 подходящего значения для него всё равно нет: `filesystem` доказывает существование файла лога, но не смысл строк, а `runtime-reflection` означает наше наблюдение живого движка, чем чтение лога не является. Трактовать как остаток NEW-01, а не как доказательство формата |
| **NEW-04** | Нет ID-вопроса «монтируется ли `MISERY-Windows.pak` в runtime фактически» | §14.7 прямо помечает это как INFERRED, confidence 0.8, «подлежит проверке в M2/M5a через I-14», но ID не назначен. Между тем на этом допущении держится **весь Tier A**: если legacy pak-путь в runtime мёртв (файл staged, но платформа pak не активна), тестовый `.pak` от `pak_writer.py` не проверяет ничего, и Tier A нужно переделывать под IoStore, что резко дороже и меняет порядок §14.7 | Завести ID в §14.3 рядом с CL-01 и закрывать его **первым** в M5a, до CT-03. Oracle: `runtime-reflection` (I-14); `container-metadata` наличие файла доказывает, но монтирование — нет |
| **NEW-05** | **Кросс-документной проверки согласованности не существует.** `tools/kb/validate.py` проверяет каждую запись по отдельности: словарь `oracle`, потолок confidence, класс утверждения, число методов, достижимость коммита. Он **структурно не способен** заметить главный вид порчи базы знаний — повышение уровня при переносе факта из документа в документ. Если `plan.md` градуирует A-07 как INFERRED 0.85, а другой документ пересказывает тот же вывод как OBSERVED 0.95, обе записи по отдельности валидны, и валидатор молчит. То же для несогласованных чисел: `repo-audit.md` RA-36 и `plan.md` — это два документа об одном объекте, и разойтись они могут без единого нарушения на уровне записи | Это не придирка к линтеру, а граница его конструкции, и `plan.md` §10.3 (правка v2.2) её теперь прямо фиксирует: единственный работающий сегодня механизм против повышения уровня — **состязательный разбор на закрытии каждого milestone**, то есть ручная процедура, зависящая от того, что её кто-то выполнит. В этом проекте она уже сработала пять раз: LOG-0006 (два дефекта в самом плане), LOG-0007 п. 8 (расхождение `DirectoryIndexSize` 844 960 против 845 472 между повторным чтением и `plan.md` A-07), re-grade `repo-audit.md` 2026-08-22, LOG-0010 (§10.3 не имел ячейки для буквального чтения бинарных данных и требовал разделения, которое сам же делал невыполнимым) и LOG-0012 (**шесть** расхождений между этим файлом и переградуированным Приложением A: A-03 0.95/0.90, A-05 0.99/0.85, A-09 0.95/0.75, A-12 0.9/0.75, A-13 0.99/0.85, Q-2.1 0.95 против пары A-04 0.85 + A-04i INFERRED 0.85 — во всех шести случаях слабее была версия в `plan.md`, то есть этот файл пересказывал факты выше их источника, ровно тот дефект, который описывает эта запись; шесть расхождений найдены одним чтением двух документов рядом и ни одно не было видно per-record валидатору). Все три находки — именно кросс-документные; ни одну из них per-record валидатор не мог найти и не найдёт. Опираться на человека там, где механизм отсутствует, законно только пока это записано как отсутствующий механизм, а не выдаётся за покрытие | **Владелец: владелец `plan.md` §10.3 и `tools/kb/validate.py`** (назначение владельца — часть регистрации этого пробела, а не следствие). **Что закроет запись:** отдельный проход-сверщик (рабочее имя `tools/kb/crosscheck.py`), который (1) собирает все записи всех документов в один индекс по id утверждения и по цитируемому объекту (id записи, хэш коммита, путь, числовое значение), (2) отвергает случай, когда одно и то же утверждение в двух документах несёт разные `evidence_level` или `confidence`, если более сильная запись не предъявляет **нового** метода, (3) отвергает расхождение числовых значений об одном объекте. Пока такого прохода нет, запись остаётся UNKNOWN, а состязательный разбор на закрытии milestone обязателен, а не рекомендован. **Что НЕ закроет запись:** расширение per-record проверок в `validate.py` любой глубины — ограничение в том, что запись сравнивается сама с собой, а не с другой записью |
---

## 9. DEBT — принято как долг по итогам аудита M1.0 (2026-08-23)

Семь находок независимого аудита закрытия M0 (`RESEARCH_LOG.md` LOG-0014). Блокеров и systemic major
среди них нет; ни одна не влияет на достоверность закрытия, безопасность или воспроизводимость.
Политика владельца проекта: такие находки записываются в долг и **не блокируют** следующий milestone.

Одна находка того же прохода в долг **не** попала и закрыта немедленно — `new_log_entry.py` не
проверял выходной путь, из-за чего универсальное утверждение `plan.md` §1.5 слоя 1 было ложным.
Обоснование в LOG-0014i п. 2.

| ID | Что | Почему это долг, а не блокер | Когда закрывать |
|---|---|---|---|
| **DEBT-01** | `LOG-0013` описывает состояние коммита `c3ece81`, а не текущего HEAD: после него было пять коммитов, три из них меняли поведение `tools/discovery/find_misery.py`, и прогон CI на `c3ece81` был красным | Вывод о закрытии подтверждён на HEAD независимым исполнением всех шести критериев (LOG-0014). Журнал append-only, поэтому запись не переписывается; расхождение зафиксировано в LOG-0014i п. 4 | Не требует правки. Учитывать при чтении: актуальные числа — в генерируемом блоке `docs/toolchain.md` §15.1 |
| **DEBT-02** | `LOG-0013` градуирован как класс P, но три из пяти его пунктов несут выводы класса I (причинность, обобщение одного эксперимента, утверждение об интервале времени вместо точки) | Числовое ядро записи действительно класс P и воспроизводится. Дефект в форме, не в содержании | При следующей записи о состоянии репозитория — разделять по образцу `LOG-0011` / `LOG-0011i` |
| **DEBT-03** | `LOG-0013i` утверждает, что §18.3 п. 5 выполнен полностью, тогда как валидатор показывает 13 `EV-04` и 31 `MD-BARE` — 44 записи с уровнем и/или confidence без oracle | Существо держится: все фактические реестры (таблицы, записи журнала, JSON-артефакты) oracle несут; эти 44 — сокращённые пересказы, а не новые неградуированные факты. Валидатор классифицирует их как предупреждения, а не нарушения | По ходу M1: при касании этих строк дописывать oracle либо снимать маркер уровня |
| **DEBT-04** | `research/evidence-model.md` (канонический пример A-07) ссылается на метод `F-02` и на сохранённый hexdump-артефакт, которых не существует: `F-02` — задача M1, `tools/fingerprint/` содержит только `.gitkeep` | Отправляет инженера искать несуществующий артефакт в документе, определяющем правила записи доказательств. Но `evidence_level`, `confidence`, `oracle` и класс совпадают с `plan.md` — §18.3 п. 6 не нарушен | **В M1.1**, вместе с реализацией `F-02`: либо ссылка станет верной, либо метод переписывается на фактически выполненный |
| **DEBT-05** | Шапка `tests/test_validator_live.py` объясняет порог `MIN_TOTAL_RECORDS=120` фразой «потеря table-row extractor роняет общее число до 111»; измерение сегодня даёт 122 — выше порога | Функциональных последствий нет: мутацией подтверждено, что гейт всё равно краснеет, но через пер-нотационный порог. Устарело только объяснение, какое именно утверждение сработало | При следующем повышении порогов по мере роста базы — файл сам это предписывает |
| **DEBT-06** | `find-misery.ps1`, запущенный как `powershell -File ... --out X`, падает с `AmbiguousParameter`: `[CmdletBinding()]` связывает `--out` как префикс `-OutVariable` раньше, чем до него доходит `ValueFromRemainingArguments` | Документированный способ вызова (`& .\...ps1 --out ...` изнутри сессии) работает и воспроизводит `install.json` точно; exit criterion M0 (3) выполнен. Задевает только автоматизацию, которая потянется к `-File`, — в репозитории такой нет | Когда появится вызывающий, которому нужен `-File`. Либо переименовать флаги, либо снять `[CmdletBinding()]` |
