# ---------------------------------------------------------------------------
# MISERY Modding Framework -- research Python environment
#
# CANONICAL INTERPRETER (the ONE interpreter for tests and for KB validation):
#     D:\Tools\venv-research\Scripts\python.exe
#
# There is exactly one. `C:\Python314\python.exe` is only the BASE from which
# the venv is created (see step 1) and must not be used to run the test suite
# or tools/kb/validate.py: it is a machine-wide interpreter whose package set
# is not described by this file, so a green run under it proves nothing about
# a fresh clone. Everything the repository ships must run under the venv.
#
# Resolved against: D:\Tools\venv-research\Scripts\python.exe
#                   CPython 3.14.0, win_amd64  (ABI tag cp314)
# Resolved on:      2026-08-22
#
# ---------------------------------------------------------------------------
# CANONICAL INSTALL SEQUENCE -- THIS FILE IS THE SINGLE SOURCE FOR IT.
#
# DO NOT install this file with a single `pip install -r requirements.txt`.
# The order matters (see the jpype1 note below).
#
# Until 2026-08-22 there were TWO orders in two documents: a three-step order
# here and a five-step order in docs/toolchain.md 8.2. Both respect the one
# real constraint (jpype1 before pyghidra), so neither was wrong -- but two
# orders in two files is how documents drift apart, and only ONE of them was
# ever actually executed. The five steps below are that one: they are the
# sequence run on 2026-08-22, whose result is the `pip list --format=freeze`
# output recorded in docs/toolchain.md 8.2. The three-step variant that used
# to stand here was never run end to end, so it is dropped rather than
# preferred for being shorter -- a tested procedure beats a tidy one.
# docs/toolchain.md 8.2 now REFERENCES these steps instead of restating them.
#
#   step 1 -- create the venv on the base interpreter (docs/toolchain.md 7.2):
#     C:\Python314\python.exe -m venv D:\Tools\venv-research
#
#   step 2 -- jpype1 FIRST, so pyghidra cannot drag in its own pinned 1.5.2:
#     D:\Tools\venv-research\Scripts\python.exe -m pip install jpype1==1.7.1
#
#   step 3 -- pyghidra from the wheel BUNDLED WITH GHIDRA, and WITHOUT
#             dependency resolution, so step 2 is not "corrected" by pip:
#     D:\Tools\venv-research\Scripts\python.exe -m pip install --no-deps "D:\Tools\ghidra_12.1.3_PUBLIC\Ghidra\Features\PyGhidra\pypkg\dist\pyghidra-3.1.0-py3-none-any.whl"
#
#   step 4 -- packaging: pyghidra needs it at runtime, but --no-deps did not
#             install it:
#     D:\Tools\venv-research\Scripts\python.exe -m pip install packaging
#
#   step 5 -- jsonschema and pytest: without them KB validation runs blind and
#             the test suite cannot run at all (docs/toolchain.md 3.1):
#     D:\Tools\venv-research\Scripts\python.exe -m pip install jsonschema==4.26.0 pytest==9.1.1
#
# Only the order of steps 2 and 3 is load-bearing. Steps 4 and 5 have no
# ordering constraints between them and do not conflict with the jpype pin
# (the single shared dependency is `packaging`, and pytest accepts >=22).
#
# Verify the environment with all three of these -- each one checks a different
# thing, and the first two are the ones that were silently broken until
# 2026-08-22 (finding MAJOR-6):
#
#   1. the package set matches this file:
#     D:\Tools\venv-research\Scripts\python.exe -m pip list --format=freeze
#
#   2. validation uses REAL jsonschema, not the built-in fallback -- the banner
#      must read "schema backend: jsonschema (full JSON Schema validation)":
#     D:\Tools\venv-research\Scripts\python.exe tools\kb\validate.py
#
#   3. the test suite runs at all (it cannot without pytest):
#     D:\Tools\venv-research\Scripts\python.exe -m pytest -q
#
# The REQUIRED result of checks 2 and 3, and the last observed ACTUAL result
# with its date, are recorded in ONE place: the acceptance checklist,
# docs/toolchain.md 15 items 9 and 10. They are deliberately not repeated here
# -- that duplication is what produced the stale "129 passed" figure this file
# carried until 2026-08-22.
#
# WHEEL HASHES ARE NOT RECORDED -- see docs/toolchain.md 6.3.
# No `--hash=` line appears below, and `pip install --require-hashes` therefore
# cannot be used with this file. That is a RECORDED DEBT under plan.md 17.3,
# not an accepted exemption: the sha256 of every wheel here is published by
# PyPI and is verifiable, we simply did not write it down at install time. The
# gap covers jpype1 1.7.1, packaging 26.3, jsonschema 4.26.0, pytest 9.1.1 and
# all eight transitive wheels. pyghidra 3.1.0 is the one exception -- its
# sha256 is recorded below, because it came from the verified Ghidra archive.
#
# HISTORY OF THIS FILE (do not silently drop): until 2026-08-22 this file
# claimed jsonschema was "added by step 1 above" while docs/toolchain.md 9.4
# recorded it as not installed. docs/toolchain.md was the accurate one: neither
# jsonschema nor pytest was present in the venv, so validate.py degraded to its
# built-in minimal validator (ignoring "format" and "propertyNames") and the
# test suite could not run under the documented interpreter at all. Both
# packages are now actually installed and pinned below.
# ---------------------------------------------------------------------------

# Java <-> Python bridge. Ghidra's scripting API is Java; PyGhidra drives it
# through JPype, so this is what actually makes pyghidra_scripts/ work.
#
# WHY THE PIN AND WHY IT MUST BE INSTALLED FIRST:
# pyghidra 3.1.0 declares JPype1==1.5.2, and JPype1 1.5.2 ships no cp314 wheel
# (its bundled wheels stop at cp313 -- see
# D:\Tools\ghidra_12.1.3_PUBLIC\Ghidra\Features\PyGhidra\pypkg\dist\). On
# Python 3.14 pip would fall back to the sdist and try to compile it. 1.7.1 has
# a cp314 wheel, so it is installed first and pyghidra is then installed with
# --no-deps so its pin is not re-resolved.
jpype1==1.7.1

# Ghidra headless / scripting from Python: opens a project, imports a binary,
# runs analysis, exposes the flat API. Version 3.1.0 is the copy BUNDLED WITH
# Ghidra 12.1.3, not the PyPI release -- keeping it in lockstep with the
# installed Ghidra is the point. The environment was built from the wheel at
#   D:\Tools\ghidra_12.1.3_PUBLIC\Ghidra\Features\PyGhidra\pypkg\dist\pyghidra-3.1.0-py3-none-any.whl
#   sha256 d85a3d52d9628146711980912884f42a1e4118e8df9d11251e9c612b3ed1ac96
# Installing this line from PyPI instead is acceptable ONLY with --no-deps.
pyghidra==3.1.0

# Version parsing and comparison. Needed wherever we reason about engine and
# tool versions as ordered values rather than as strings: UE 5.4.4 vs 5.4.x
# compatibility checks (plan.md 16), Ghidra/JDK requirement checks, build
# comparison in tools/fingerprint. Also a transitive dependency of pyghidra
# and of pytest. Left unpinned deliberately -- nothing here is sensitive to its
# version. (26.3 is what resolved in venv-research on 2026-08-22.)
packaging

# JSON Schema validation -- the engine behind tools/kb/validate.py (plan.md
# K-03): every machine-readable artifact in research/ is validated against a
# schema in research/schema/ before it is trusted.
# WHY PINNED AND WHY IT IS NOT OPTIONAL: validate.py imports jsonschema and,
# if the import fails, falls back to a built-in minimal validator that does NOT
# enforce "format" or "propertyNames". That fallback exits 0, so a missing
# jsonschema does not look like a failure -- it looks like a pass. K-03's exit
# criterion ("validation runs clean") is therefore only meaningful when the
# validator banner reports the jsonschema backend.
# Installed and verified in venv-research on 2026-08-22.
jsonschema==4.26.0

# Test runner for tests/ (tests/test_kb.py, tests/test_inventory.py, and every
# test module added later). Pure-Python, no MSVC needed (docs/toolchain.md 2.1).
# WHY PINNED: this is the only way the repository's own tests can be executed,
# and until 2026-08-22 it was absent from the documented interpreter, so the
# suite ran only by accident under a different, undocumented Python.
# Installed and verified in venv-research on 2026-08-22. The pass/fail counts
# of the last actual run live in docs/toolchain.md 15 item 10 and nowhere else;
# the figure "129 passed" that stood here was already stale when it was written
# down twice.
pytest==9.1.1
